Your task is to efficiently calculate values a^b modulo 10^9+7.
Note that in this task we assume that 0^0=1.

Input:
The first input line contains an integer n: the number of calculations.
After this, there are n lines, each containing two integers a and b.

Output:
Print each value a^b modulo 10^9+7.

Constraints:
1<=n<=2*10^5
0<=a,b<=10^9

Strategy:
We use modular exponentiation which can be facilitated by using the fact that x^n=x^(n/2+n/2)=x^n/2*x^n/2 is x is even and x^n=x*x^n/2*x^n/2 if x is odd. We can write a recursive function with base case at exponent == 0 (return 1) and then for odd exponent we return f(a, b-1)*a and for even exponents we save the result of f(a,b/2) and return res*res. We apply modulo in each step.