Given an array of n integers, your task is to process q queries of the form: what is the sum of values in range [a,b]?

Input:
The first input line has two integers n and q: the number of values and queries.
The second line has n integers x_1,x_2,...,x_n: the array values.
Finally, there are q lines describing the queries. Each line has two integers a and b: what is the sum of values in range [a,b]?

Output:
Print the result of each query.

Constraints:
1<=n,q<=2*10^5
1<=x_i<=10^9
1<=a<=b<=n

Strategy:
We can solve this with a simple prefix sum array. We make it size n+1 pref[0]=0 so we can do sums from a==1. Then we print pref[b]-pref[a-1].